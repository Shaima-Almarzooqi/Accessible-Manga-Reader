wxpython>=4.2
requests>=2.31
pillow>=10.0

# PDF import. pypdfium2 ships wheels for every Windows architecture,
# including ARM64, so PDF import works on all builds.
pypdfium2>=4

# Tagged PDF export uses Microsoft Edge (or another installed Chromium
# browser) as the renderer. pypdfium2 above verifies that the result has
# the structure tree a screen reader needs, so no second PDF dependency
# or bundled native rendering runtime is required.

# MP3 encoding for the audio export. Wheels exist for both
# Windows architectures, and it is about 150 KB.
lameenc>=1.8

# Kokoro speech synthesis. The model files are downloaded separately on first
# use and stored under the app data folder. These versions have native wheels
# for both Windows x64 and ARM64. Mandarin uses Kokoro's dedicated frontend on
# the Python 3.12 packaged builds; Python 3.13 development environments use the
# eSpeak frontend included by kokoro-onnx.
kokoro-onnx==0.5.0
onnxruntime==1.28.0
misaki-fork[zh]==0.9.6; python_version < "3.13"

# Reaching the speech voices already on the computer, so a book can
# be read aloud with no key and no allowance. Pure Python, so it
# works on every architecture.
comtypes>=1.4
